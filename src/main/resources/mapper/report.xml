<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fastcampus.group9.toyproject3admin.domain.report.ReportRepository">
    <insert id="insert">
        insert into report_tb(board_id, is_reported, reportContent, created_at)
        values (#{boardId}, true, #{reportContent}, now())
    </insert>
</mapper>